apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-config
  namespace: media
  labels:
    app: calibre
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: local-path
  