apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cwa-plugins
  namespace: media
  labels:
    app: cwa
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: local-path
  