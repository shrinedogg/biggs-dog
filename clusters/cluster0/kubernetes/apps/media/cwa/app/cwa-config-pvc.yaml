apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cwa-config
  namespace: media
  labels:
    app: cwa
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-block
  