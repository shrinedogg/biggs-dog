apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ersatz-config-pvc
  namespace: media
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi