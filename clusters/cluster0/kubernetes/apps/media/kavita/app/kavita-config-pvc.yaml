apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-config
  namespace: media
  labels:
    app: kavita
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: local-path
  