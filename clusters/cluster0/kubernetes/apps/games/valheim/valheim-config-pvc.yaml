apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valheim-config-pvc
  namespace: valheim
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi